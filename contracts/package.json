{
  "name": "ai-memory-vault-contracts",
  "version": "1.0.0",
  "description": "AI Memory Vault - Smart contracts for decentralized AI agent memory on 0G Chain",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network 0g-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network 0g-mainnet",
    "verify:testnet": "hardhat verify --network 0g-testnet",
    "verify:mainnet": "hardhat verify --network 0g-mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "ts-node": ">=8.0.0",
    "typescript": ">=4.9.0"
  }
}
